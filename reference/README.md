# Reference Assets

This directory stores non-Sphinx reference materials used during development, for example:

- vendor/user-guide PDFs
- data-package PDFs
- metadata/data-dictionary PDFs

These files are intentionally kept outside `docs/` so the `docs/` tree remains strictly
Sphinx source content (`.rst`, config, and build inputs).
